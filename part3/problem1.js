function playWithArterisk(nilai) {
    let result = "";
    for (let i = 0; i < nilai; i++) {
        result += "*"
        console.log(result);
    }

    return "==="
}

console.log(playWithArterisk(5));